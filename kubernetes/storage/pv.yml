apiVersion: v1
kind: PersistentVolume

metadata:
  name: postgres-pv

spec:
  storageClassName: ""
  capacity:
    storage: 2Gi

  accessModes:
    - ReadWriteOnce

  hostPath:
    path: /tmp/postgres-data
